import { createStore } from 'vuex';

export type State = { };

const state: State = { };

export default createStore({
  state,
  mutations: {
  },
  actions: {
  },

  getters: {
  },
  modules: {

  },
});
